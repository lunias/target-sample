package com.ethanaa.target.sample.l1message.type;

/**
 * Enum which defines the types of RESTful HTTP method invocations which can be sent to an L1 application.
 */
public enum L1RESTRequest implements L1MessageType {

    GET, PUT, PATCH, POST
}
